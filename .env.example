# "openai" (paid, no free tier) or "groq" (free tier, good for testing at zero cost)
DEEPWAVE_LLM_PROVIDER=groq

#OpenAI (used when DEEPWAVE_LLM_PROVIDER=openai)
OPENAI_API_KEY=sk-your-key-here

#Groq (used when DEEPWAVE_LLM_PROVIDER=groq)
# Free key: https://console.groq.com
# GROQ_API_KEY=gsk-your-key-here
